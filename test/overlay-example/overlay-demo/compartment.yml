# compartment.yml — демо compartment overlay (L3-mesh через tailscale-сайдкар)
name: overlay-demo
secrets:
  from: secrets.sops.yaml   # ключи из этого SOPS-файла (по умолчанию secrets.sops.yaml)
  create:
    - ts-authkey            # только этот ключ становится podman-секретом;
                            # overlay-ts.container ссылается на него через Secret=ts-authkey
